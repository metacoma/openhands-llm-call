[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "integration: tests that start local HTTP/MCP subprocesses",
]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio"]
